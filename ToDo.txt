Alias:
Done

Dhcpd:
Done

DhcpStaticMap:
Done

DnsResolver:
crashes on pfsense_api line 121 at hosts: Cannot find an overload for "Add" and the argument count: "2"

DnsResolverHost:
crashes (AliasesDescription = "aliases/item/Description" still has a xmlelement)
It looks to happen if a host has no aliases

Firewall:

Gateway:

Interface:

Portfwd:
Source Type: 
    if sourcetype is network, then a local network name is used, like opt1. if opt1ip then it is still network, but with a opt1 address. the network can also be self for this firewall
    if sourcetype is address, a ipaddress our networkaddress with subnet can be enterd.
Print:
Add:
Edit:
Delete:

StaticRoute:
Done

Vlan's:
Empty.

